import "./_styles.scss";

export const Header = {
  name: "Header",

  render() {
    return (
      <header>
        <nav>
          <div class="logo">Logo</div>
        </nav>
      </header>
    );
  },
};

export default Header;
